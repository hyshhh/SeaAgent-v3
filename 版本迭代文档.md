# SeaAgent 版本迭代文档

每次上传代码时追加一节，记录三样：**GitHub 提交链接、与上一版的优化点、当前版本业务流程。**

## 版本索引

| 版本 | 主题 | 提交 | 日期 |
| ---- | ---- | ---- | ---- |
| v3.1 | 节点间改显式图边：LangChain 建 Agent，LangGraph 编排 Agent | [e48c566](https://github.com/hyshhh/SeaAgent-v3.1-based_LG/tree/e48c566) | 2026-09-13 |
| v2.0 | 四 Agent 图式协同与工具执行者 | [461409d](https://github.com/hyshhh/SeaAgent-v3/tree/461409d) | 2026-09-13 |

---

## v3.1 主要优化点

- **协同关系落到图结构**：v2.0 把四个 Agent 送进了图，但节点之间其实**没有任何边** —— 去向由节点体内 7 处 `return Command(goto=...)` 硬编码决定。本版把这些全部移除，改为 **2 条静态边 + 2 条条件边**：

  | 边 | 类型 | 依据 |
  | -- | ---- | ---- |
  | `intent → plan` | 静态边 | 无条件 |
  | `plan → observe \| reflect` | 条件边 `_route_plan` | 读 `state["active_agent"]` |
  | `observe → reflect` | 静态边 | 无条件 |
  | `reflect → plan \| END` | 条件边 `_route_reflect` | 读 `state["reflection"]["state"]` |

  节点只把结论写回状态，**不再返回 Command 决定去向**；两个路由函数只做转发，不含业务判定。这对应「LangChain 负责构建 Agent，LangGraph 负责编排 Agent」的分工。

- **确定性业务规则外移**：时间守卫、重规划指令族、计划生成与校验、验收清单共 **16 个纯函数 + 1 个常量（884 行）**搬到新文件 `agent/planning.py`，`graph.py` 只留状态契约、图装配与四个节点。为不打断既有测试的 `from agent.graph import _xxx`，在 `graph.py` 顶部做**显式具名再导出**（不能用 `import *`，下划线私有名不会被带出）。

- **技能改为「一技能一工具」**：原先技能由**代码预选**（`always` 无条件 + 关键词字面匹配）后注入 system prompt，未选中的只给一行文本目录，另有一个统一的 `loadSkill(skillId)` 工具。现在**每个技能包装成一个独立工具** `load_<skill_id>`，工具描述即技能简介 —— 模型看到的工具列表本身就是技能目录，调用才取回正文；system prompt 里不再有任何技能内容，代码也不再做关键词预选（那套匹配是 `token in text` 的字面包含，高频词会让技能被普遍命中）。`skill_loader.py` 由 341 行瘦到 **142 行**，四个 `catalog.yaml` 删掉 `always`/`when`/`match_*`/`tags` 全部字段，原「适用场景」并入 `description`（它现在是模型判断要不要读的唯一依据）。

  配套改动：`reflect` 的 ReAct 步数上限 6 → 12（技能工具从 1 个变 5 个，一轮「模型→工具」往返耗 2 步，原预算读完两个技能就来不及调 `submit_verdict`）；前端不再把「挂载的技能」当成「已读」显示，并按 `skillId` 去重（原先按 `skillId:source` 去重，会把同一技能算两次）；技能正文缺失时跳过挂载并记 warning，不再让整条技能通道无声消失。

- **死代码清理**：`lc_tools.py` 删除无调用方的 `build_observe_tools`、其专属 11 个 Args 类与结果压缩函数（**510 → 124 行**，后随技能工具化增至 151 行）；`graph.py` 删除 `_tool_event`；`AgentState` 删除从未读写的 `error` 字段；全项目扫描后又清掉 `clear_skill_cache`、`_is_browser_compatible`、`focus_blob`、前端 `liveSkillSnapshot`/`currentSkillSnapshot` 与孤儿目录 `skills/controller/`。

- **状态契约纠偏**：`plan_repair` / `plan_used_default` 原先由 `plan_node` 写入、`reflect_node` 读回，却**未声明在 `AgentState`** 中。LangGraph 只为自己 schema 声明过的键建 channel，未声明的写入会被静默丢弃 —— 即这两个值过去一直是空值，落库的 `planRepair` 与决策指标恒为 0。本版补上声明与初值，**属有意的行为修正**。

## 业务流程

```
用户提问
   ↓
Intent Agent ──静态边──→ Plan Agent
                            │ 条件边：读 active_agent
              ┌─────────────┴─────────────┐
              ↓                           ↓
        Observe Agent ──静态边──→    Reflect Agent
                                          │ 条件边：读 reflection.state
                            ┌─────────────┴─────────────┐
                            ↓                           ↓
                    Plan Agent（replan）              END
                                                        ↓
                                              Controller 落库与结论
```

与 v2.0 的差别不在流程形状，而在**这些跳转现在画在图上，而不是写在节点里**。

## 本版已知的行为变化

目标是对外可观察行为不变。有两处差异，均不影响前端事件流、`answer()` 返回结构与最终结论：

1. **`planRepair` / `planUsedDefault` 由恒定空值恢复为真实值**（见上「状态契约纠偏」）。影响面仅限 `qa_rounds` 表与分析指标 `decisionMetrics`。若要与 v2.0 严格一致，删掉 `AgentState` 中这两行声明即可退回原行为。
2. **`active_agent` 的初值与终值改变**：写点由 4 处收敛到 `plan_node` 1 处，故初值由 `"intent"` 变为 `""`、常规结束时的终值由 `"reflect"` 变为 `"observe"`。该键是本次新增的 plan 条件边路由键，controller / web / tests 均无读取方，不可观察。

## 验证状态

本版**未在本机运行 pytest** —— 开发机只装了 Python 3.12 且没有 langchain / langgraph，无法导入图。已完成的验证是代码级的：

- 三个改动文件编译通过；`symtable` 静态扫描**无悬空引用**；
- 搬迁段与原 `graph.py` 对应区间 **sha256 逐字节相同**（884 行），AST 树结构一致；
- `tests/` 下全部 `from agent.graph import ...` 的**名字均可解析**；
- 被删符号全仓 grep 无运行时引用；
- 条件边取值时机（分支函数在源节点 update 应用之后求值）依据 LangGraph 官方语义确认，未实测。

**待在有依赖的环境验证**：20 个 pytest 全部通过，以及上述第 1 条行为变化是否符合预期。

## v2.0 主要优化点

- **协同层显式化**：由主程序依次调用独立 Agent，改为 **LangGraph `StateGraph` 编排**，Intent / Plan / Observe / Reflect **四 Agent 入图**，协作关系由图结构与路由表达。
- **执行入口统一**：Agent 间数据流转收敛到 **`submit_plan`** 一个工具调用，调用序列由**工具执行者**统一求值、补参、校验。
- **`$ref` 引用**：模型只写引用路径，真实数据由执行者从历史结果取值，**省去长上下文复述**。
- **契约校验前置**：不合法参数在执行前拦下并记入 **`tool_records`**，供 Reflect 检出。
- **持久化外移**：图内不落库，QA 轮次由**图外的 Controller** 写入 `qa_rounds` 等记忆表。

## 业务流程

```
用户提问
   ↓
Intent Agent → Plan Agent → submit_plan
                                 ↓
                           工具执行者
                                 ↓
                          Observe Agent
                                 ↓
                          Reflect Agent
                            ↙        ↘
                    未满足 → replan   满足 → END
                                             ↓
                                    Controller 落库与结论
```

Reflect 判为未满足则 replan 回 Plan，满足则出图，由 Controller 落库并合成结论。

## 工具执行者

位于 Plan 与 Observe 之间，把**模型写出的引用与参数变成可执行的真实调用**：`$ref` 求值、参数富化、契约校验。序列来源两条——**Plan 生成**，或 **`_default_plan_calls` 确定性兜底**，都要过这三层。

### `$ref` 求值

序列格式由 **`SubmitPlanArgs.calls`** 定义，每项含 `id`（可选，缺省补 `step-N`）、`tool`、`arguments`（字面量或 `$ref`）、`condition`（可选，不满足跳过）。**路径写法：`步骤id.字段名`。**

「在库船列表」四步链（[graph.py#L730](https://github.com/hyshhh/SeaAgent-v3/blob/461409d/agent/graph.py#L730)）：

```python
[
  {"id": "registry", "tool": "listRegistry", "arguments": {}},

  {"id": "tracks",   "tool": "getTrack",     "arguments": {"offset": 0, "limit": 0}},

  {"id": "frames",   "tool": "getFrames",
   "arguments": {"trackIds": {"$ref": "tracks.trackIds"}},
   "condition": {"ref": "tracks.trackIds"}},

  {"id": "match",    "tool": "matchImage",
   "arguments": {
     "queryImages":   {"$ref": "registry.registryReferences"},
     "galleryImages": {"$ref": "frames.keyframes"},
     "registryItems": {"$ref": "registry.registryItems"},
     "topK": 0
   },
   "condition": {"ref": "frames.keyframes"}},
]
```

| 步骤 | 动作 | 参数来源 |
| ---- | ---- | -------- |
| `registry` | 列出先验库 | 无 |
| `tracks` | 全量扫描轨迹 | 无 |
| `frames` | 给轨迹取关键帧 | `tracks.trackIds`；取不到则跳过 |
| `match` | 库图与关键帧做图像匹配 | `registry` 库图 + `frames` 关键帧；后者为空则跳过 |

### 参数富化

**由代码主动执行。**模型没给全的参数，从本轮 scope 里捞出补上（P4）。模型只表达调用意图，**能从上一项结果推出的参数不必重写**：

```python
{"topK": 5}                                   # 模型只写了这些
{"topK": 5, "galleryImages": [ ...库参考图 ]}   # 富化后
```

### 契约校验

**由代码强制执行。**三层依次过滤（P3），不通过则 skip 并写入 `tool_records`：

| 层 | 检查内容 |
| -- | -------- |
| 白名单 | 有没有多余参数 |
| 必填 | 该给的参数给了没有 |
| 解析后取值 | 值本身是否有效 |

Reflect Agent 检出 skip 记录后，决定是否补全调用。
